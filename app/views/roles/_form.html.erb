<%= form_with(model: role) do |form| %>
  <% if role.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
        <% role.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "Nombre rol", style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <br>

  <div>
    <%= form.submit "Aceptar", class:"btn btn-success btn-sm" %>
      </div>

      <br>

      <div>
        <%= link_to 'Volver', roles_path, class:"btn btn-outline-primary btn-sm" %>
      </div>

<% end %>



