<%= form_with(model: morning_time) do |form| %>
  <% if morning_time.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(morning_time.errors.count, "error") %> prohibited this morning_time from being saved:</h2>

      <ul>
        <% morning_time.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :m_time, style: "display: block" %>
    <%= form.time_field :m_time %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
