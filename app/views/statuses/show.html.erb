<p style="color: green"><%= notice %></p>

<%= render @status %>

<div>

  <% if user_is_admin? %>
    <%= link_to "Editar este status", edit_status_path(@status) %> |
  <% end %>

  <%= link_to "Volver a statuses", statuses_path %>

  <% if user_is_admin? %>
    <%= button_to "Eliminar este status", @status, method: :delete %>
  <% end %>

</div>
