<title>Agendar cita &middot; Clínica Dental San Antonio</title>

<% if user_is_patient? %>

  <div class="page-content page-container" id="page-content">
    <div class="padding">
      <div class="row container d-flex justify-content-center">

        <div class="col-lg-10 grid-margin stretch-card">
          <div class="card">
            <div class="card-body">
              <div class="col-sm-7"><h2>Agendar <b>cita</b></h2></div>
              <p class="card-description">
                Selecciona un dentista.
              </p>

  <section class="pt-4">
    <div class="container px-lg-5">
      <div class="row gx-lg-5">

        <% @dentists.each do |dentist| %>

          <div class="col-lg-6 col-xxl-4 mb-5">
            <div class="card bg-light border-0 h-100">
              <div class="card-body text-center p-4 p-lg-5 pt-0 pt-lg-0">
                <div  class="feature bg-primary bg-gradient text-white rounded-3 mb-4 mt-n4"><img src="/assets/Dentist_Appointment.png" width="40px" height="40px"></div>
                <h2 class="fs-4 fw-bold"><%= "Dra. " + dentist.name + " " + dentist.last_name %></h2>
                <p class="mb-0"><%= dentist.rut %></p>

                </br>

                <%= link_to "Seleccionar", new_appointment_path(:fecha_t => @fecha_recibida, :dentist_t => dentist.id), class: "btn btn-primary btn-sm" %>
              </div>
            </div>
          </div>

        <% end %>

      </div>
    </div>
  </section>
              <%= link_to "Volver a seleccionar fecha", agendar_path, class: "btn btn-info btn-sm" %>
              <%= link_to "Cancelar", calendar_path, class: "btn btn-outline-danger btn-sm" %>

            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

<% end %>