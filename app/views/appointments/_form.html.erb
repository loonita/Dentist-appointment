<%= form_with(model: appointment, local: true, html: { class: "form" }) do |form| %>
  <% if appointment.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(appointment.errors.count, "error") %> prohibited this appointment from being saved:</h2>

      <ul>
        <% appointment.errors.full_messages.each do |error| %>
          <li><%= error %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <% if user_is_admin? || user_is_secretary? || user_is_dentist? %>
    <div class="form-group">
      <%= form.label "Paciente" %>
      <%= form.collection_select :user_id, User.where(role_id: 1), :id, :name, class: "form-control" %>
    </div>

  <% else %>
    <%= form.hidden_field :user_id, value: current_user.id %>
  <% end %>

  <div class="form-group">
    <%= form.label "Dentista" %>
    <%= form.collection_select :dentist_id, User.where(role_id: 2).order(name: :asc), :id, :name_dentist, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label "Fecha" %>
    <%= form.date_field :date, class: "form-control small-input" %>
  </div>

  <div class="form-group">
    <%= form.label "Hora" %>
    <%= form.time_select :time, { minute_step: 30, start_hour: 9, end_hour: 17 }, { class: "form-control small-input" } %>
  </div>



  <% if user_is_admin? || user_is_secretary? || user_is_dentist? %>
    <div class="form-group">
      <%= form.label :status %>
      <%= form.select :status_id, options_from_collection_for_select(Status.all, 'id', 'name') %>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.submit "Guardar", class: "btn btn-primary" %>
  </div>

<% end %>